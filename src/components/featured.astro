---
import { Image } from "astro:assets"
import AddToCartButton from "@/components/add-cart-button.tsx"
import { Card,CardContent,CardFooter } from "./ui/card";
import HeadPhone from "@/assets/sen-headphones.avif"
import HeadPhoneTwo from "@/assets/headphones-four.avif"
import Cables from "@/assets/cables.avif";
import MousePad from "@/assets/mouse-pad.avif";
import type { Product } from "@/lib/models";
import type { ImageMetadata } from "astro";

const products: Product[] = [
  {
    id: "1",
    name: "AudioPhile Pro",
    category: "Over-Ear",
    price: 299.99,
    rating: 4,
    src: HeadPhoneTwo,
  },
    {
        id: "2",
        name: "UltraFast USB-C",
        category: "New",
        price: 19.99,
        rating: 5,
        src: Cables,
    },
  {
    id: "3",
    name: "AudioPhile Pro",
    category: "Over-Ear",
    price: 299.99,
    rating: 4,
    src: HeadPhone,
  },
  {
    id: "4",
    name: "ComfortMouse Pad XL",
    category: "Mousepads",
    price: 39.99,
    rating: 5,
    src: MousePad,
  },
    {
        id: "5",
        name: "UltraFast USB-C",
        category: "New",
        price: 19.99,
        rating: 5,
        src: Cables,
    },
  {
    id: "6",
    name: "ComfortMouse Pad XL",
    category: "Mousepads",
    price: 39.99,
    rating: 5,
    src: MousePad,
  },
]
---

<section class="bg-zinc-100 py-16 min-h-[28rem]">
    <h2 class="text-[55px] text-center font-extrabold text-zinc-900 mb-6 text-pretty tracking-tight">Featured</h2>
        <div class="flex flex-col md:flex-row md:flex-wrap justify-center items-center">
          {products.map((product) => (
          <Card className="rounded-none bg-zinc-100 max-w-sm mx-auto">
                <CardContent className="min-h-[8rem] p-5">
                    <Image src={product.src as ImageMetadata} alt={product.name} width={900} height={900} class="w-full h-56" />
                </CardContent>
                <CardFooter className="flex flex-col items-start gap-y-1">
                    <h1 class="font-bold text-xl">
                        {product.name}
                    </h1>
                    <h3 class="text-secondary-foreground/90 font-light">${product.price.toFixed(2)}</h3>
                    <AddToCartButton product={{...product, quantity: 1}} client:load/>
                </CardFooter>
            </Card>
          ))}
      </div>
    </section>
